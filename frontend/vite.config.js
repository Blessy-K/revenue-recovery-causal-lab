import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  preview: {
    allowedHosts: ['revenue-recovery-causal-lab-frontend.onrender.com'],
  },
})